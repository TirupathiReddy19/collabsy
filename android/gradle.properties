org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Kotlin's incremental-compilation cache computes relative paths between the
# pub-cache (often on a different drive letter on Windows, e.g. C:) and this
# project's build dir (e.g. D:) - that throws when the two roots differ, so
# incremental compilation is disabled for Windows cross-drive setups.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
