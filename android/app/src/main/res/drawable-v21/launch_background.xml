<?xml version="1.0" encoding="utf-8"?>
<!-- Matches SplashBackground's base color and the Collabsy mark, so this
     native launch screen hands off to the Dart splash screen
     (lib/features/splash/splash_screen.dart) without a white flash or a
     color/logo mismatch between the two. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/ic_launcher_foreground" />
    </item>
</layer-list>
